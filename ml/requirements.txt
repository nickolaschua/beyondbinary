# SenseAI ML Pipeline — Python 3.12 compatible
# mediapipe 0.10.21 is the last version with mp.solutions.holistic (legacy API).
# mediapipe 0.10.21 requires numpy<2, so tensorflow pinned to 2.16.2 (numpy<2 compatible).
# opencv-contrib-python is pulled in by mediapipe; we also install opencv-python for cv2.
mediapipe==0.10.21
tensorflow==2.16.2
numpy==1.26.4
opencv-python==4.11.0.86
fastapi==0.128.6
uvicorn==0.40.0
websockets==16.0
python-multipart==0.0.22
scikit-learn==1.6.1
matplotlib==3.10.8
