services:
  ralph:
    build: .
    volumes:
      - .:/workspace
    working_dir: /workspace
    env_file:
      - .env
    environment:
      - HOME=/home/ralph
    user: "ralph"
    command: ["./scripts/ralph/ralph.sh", "--tool", "claude", "${RALPH_ITERATIONS:-20}"]
